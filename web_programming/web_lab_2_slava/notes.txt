ssh -NT -L 13120:localhost:13120 -p 2222 s409946@helios.cs.ifmo.ru

tail -f ~/wildfly/standalone/log/server.out

Добавил JSON-ответ в AreaCheckServlet.java (lines 17-63): при mode=ajax сервер возвращает последние и все накопленные точки, иначе остаётся переход на result.jsp.
scripts/index.js (lines 1-120) теперь отправляет форму через fetch, обновляет таблицу и перерисовывает канвас без перезагрузки. Ввёл helpers для форматирования/отрисовки истории.
scripts/canvas.js (lines 1-171) получил сетку, подписи 1…4, цветные точки (зелёный попадание, красный мимо) и глобальную функцию updateCanvasPoints, чтобы фронт сразу отображал свежие попадания.
scripts/utils.js (lines 1-35) немного доработан для работы со скрытым полем x (чтобы AJAX отправлял любое значение).
Форматирование координат в таблицах через String.format(…, \"%.4f\", …) (index.jsp (lines 1-110), result.jsp (lines 1-60)) избавляет от экспоненциальной записи.
Чтобы увидеть изменения на Helios:

./gradlew clean build
./deploy-wildfly.sh
Открой страницу и протестируй клики (без выбранного R всплывает ошибка, с выбранным — точки уходят через AJAX и сразу появляются в таблице/на графике).
Если понадобится, можно дополнительно обработать ошибки fetch (сейчас просто выводится общее сообщение).




без новой таблицы, всё на старой странице
[done] точку красить в цвет, если попали 
[done] провести линии координатной сетки, обозначить 1, 2, 3, 4 значения R 

