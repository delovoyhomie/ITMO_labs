* поставить ограничения диапазона ввода 
* увеличить окно графика, чтобы не выходило за пределы
* ставить точку на графике 


curl -s "http://localhost:12000/fcgi-bin/app.jar?x=0&y=0&r=1"

rsync -av -e 'ssh -p 2222' static/ s409946@helios.cs.ifmo.ru:~/.www/
scp -P 2222 build/libs/app.jar s409946@helios.cs.ifmo.ru:~/httpd-root/fcgi-bin/app.jar


ssh -NT -o ServerAliveInterval=60 -o ServerAliveCountMax=5 \
    -L 12001:localhost:12000 -p 2222 s409946@helios.cs.ifmo.ru

фиксы:
* дробные значения 


вопросы:
* как написать действительно fastcgi сервер? потому что сейчас у меня реализовано будто cgi обычный
из-за System.getProperty("QUERY_STRING"); у меня если запросы придут в 1 милисекунду (в один момент),
то один запрос может потеряться и нужно сделать так, чтобы он не терялся (то есть работал асинхронно)
* почему в javascript синтаксическим сахаром называется классическое наследование, 
а не прототипное? 
* почему в javascript создали прототипное наследование, а не классическое?
* как сделать горизонтальное масштабирование с помощью fastcgi?

рассказать как это сделать у меня 







